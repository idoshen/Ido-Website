import React from 'react';
import './HTTP-Server.css';

function HTTPServer(){
  return (
    <section id="HTTPServer" className="section HTTPServer fade-in">
      <h1>HTTP Server</h1>
    </section>
  );
}

export default HTTPServer;
